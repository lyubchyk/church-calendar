<%
  custom = { :rows => @resource[:class].typus_options_for(:form_rows) }
  options = options.merge!(custom)
%>

<li>
  <%= form.label attribute, label_text %>
  <%= fckeditor_textarea(:item, attribute, :toolbarSet => "Simple", :width => '100%', :height => '350px') %>
</li>